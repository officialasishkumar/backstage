---
'@backstage/plugin-catalog-node': minor
---

Added `CatalogModelRegistryService` and `catalogModelRegistryServiceRef` that collects catalog model fragments from plugins and compiles them into a unified catalog model at startup.
